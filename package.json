{
  "name": "qrcode-generator-e2e",
  "private": true,
  "scripts": {
    "build": "NO_COLOR=true trunk build --release",
    "check": "pnpm run check:rust && pnpm run check:web && pnpm run check:python && pnpm run build",
    "check:python": "uv run --project tests/oracles --locked ruff check tests/support && uv run --project tests/oracles --locked ruff format --check tests/support && uv run --project tests/oracles --locked ty check tests/support",
    "check:rust": "cargo fmt --check && cargo check && cargo check --target wasm32-unknown-unknown && cargo clippy --all-targets --all-features -- -D warnings",
    "check:web": "pnpm run lint && pnpm run format:check",
    "dev": "NO_COLOR=true trunk serve --open",
    "format": "cargo fmt && oxfmt e2e playwright.config.ts package.json && pnpm run format:python",
    "format:python": "uv run --project tests/oracles --locked ruff format tests/support",
    "format:check": "oxfmt --check e2e playwright.config.ts package.json",
    "lint": "oxlint --deny-warnings e2e playwright.config.ts",
    "setup": "bash scripts/setup.sh",
    "test": "pnpm run test:rust && pnpm run test:python && pnpm run test:wasm && pnpm run test:e2e",
    "test:e2e": "playwright test --project=chromium --project=mobile-chromium",
    "test:e2e:all": "playwright test",
    "test:python": "uv run --project tests/oracles --locked python -m unittest discover -s tests/support -p 'test_*.py'",
    "test:rust": "cargo test",
    "test:wasm": "cargo test -p qr-web --target wasm32-unknown-unknown --lib && cargo test -p qr-web --target wasm32-unknown-unknown --test wasm_browser",
    "test:approved": "cargo test -p qr-render --test approved_combinations --test resource_baselines --test adverse_transforms",
    "test:decode": "QR_RELEASE_EVIDENCE_DIR=target/release-evidence cargo test -p qr-render --test independent_png_decode --test independent_svg_decode --test logo_decode -- --ignored",
    "test:adverse:decode": "QR_RELEASE_EVIDENCE_DIR=target/release-evidence cargo test -p qr-render --test adverse_transforms -- --ignored",
    "release:bundle": "bash scripts/verify-bundle-size.sh",
    "release:coverage": "bash scripts/verify-coverage.sh",
    "release:dependencies": "cargo audit && cargo tree --workspace --duplicates",
    "release:evidence": "bash scripts/release-evidence.sh",
    "release:fuzz": "cargo fuzz run encode_utf8 -- -max_total_time=600 && cargo fuzz run encode_valid_text -- -max_total_time=600 && cargo fuzz run bit_buffer -- -max_total_time=600 && cargo fuzz run reed_solomon -- -max_total_time=600 && cargo fuzz run matrix_build -- -max_total_time=600 && cargo fuzz run render_svg -- -max_total_time=600 && cargo fuzz run render_png -- -max_total_time=600 && cargo fuzz run fixture_parser -- -max_total_time=600",
    "release:fuzz:deep": "cargo fuzz run encode_utf8 -- -max_total_time=3600 && cargo fuzz run reed_solomon -- -max_total_time=3600 && cargo fuzz run matrix_build -- -max_total_time=3600 && cargo fuzz run render_png -- -max_total_time=3600",
    "release:miri": "cargo +nightly miri test -p qr-core && cargo +nightly miri test -p qr-render --lib && cargo +nightly miri test -p qr-render --test profile_geometry --test render_model",
    "release:mutation": "bash scripts/verify-mutations.sh",
    "release:performance": "cargo bench -p qr-render --bench encode_render",
    "release:readiness": "bash scripts/release-readiness.sh",
    "release:size-attribution": "cargo bloat --release --package qr-web --target wasm32-unknown-unknown --crates",
    "verify": "pnpm run check && pnpm run test"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@playwright/test": "1.62.1",
    "oxfmt": "0.62.0",
    "oxlint": "1.77.0",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": "24",
    "pnpm": ">=11.20.0 <12"
  },
  "packageManager": "pnpm@11.20.0"
}
