# 27 — Harden the complete branded output matrix

**What to build:** Make the combined compact dots, prominent corner finders, branded minimum version, and enlarged centered logo release-ready through exhaustive automated evidence and current user guidance.

**Blocked by:** 26 — Enlarge the exactly centered ONE logo.

**Status:** resolved

- [x] The generated approved-output matrix covers every profile, background treatment, logo state, required payload class, enabled version, and intentional branded-geometry rejection without leaving an unrepresented selectable path.
- [x] Native PNG and independently rasterized SVG artifacts pass the pinned decoder for all accepted rows and record deterministic safety classifications and geometry facts.
- [x] Deterministic adverse testing exercises blur, down/up scaling, JPEG screenshots, rotation, perspective, contrast, brightness, transparent surfaces, print dot gain, ink loss, and grayscale with a documented pass envelope appropriate to compact dots and logo cautions.
- [x] Desktop Chromium proves real-size preview, prominent square finders, compact dots, minimum-version refitting, exact logo centering, deterministic downloads, and zero production payload/logo requests without retries; no responsive-layout assertion is required.
- [x] Generated logo policy, development policy, testing strategy, release hardening guidance, local ticket answers, and the implementation map describe the final approved behavior consistently.
- [x] Release automation retains reproducible hashes, privacy, downloads, decoder evidence, and guidance while not restoring manual-test evidence, bundle-size testing, or the removed automated accessibility test.
- [x] All repository verification commands relevant to Rust, WASM, web, Chromium tooling, Python support, fuzz targets, deterministic artifacts, and independent decoding pass before resolution; no performance benchmark is required.

## Answer

The release matrix now contains 248 stable, unique scenario rows generated from
the compiled product surface: 96 required-payload rows and 152 exact-version
rows. Every row carries matched native-PNG and independently rasterized SVG
evidence. The pinned ZXing-C++ reader decodes all 142 accepted rows in both
formats; the other 106 rows prove their expected typed rejection. Deterministic
artifact and decoder-input hashes, safety classifications, selected versions,
dimensions, and the exact reviewed Version 6 logo placement travel together in
the merged schema-2 evidence.

The adverse manifest now owns three explicit pass envelopes and the readiness
validator rejects missing or drifted membership: all 13 transforms decode for a
low-density opaque Print compact-dot symbol, 10 decode for transparent compact
dots, and six decode for the centered Version 6 Print logo, for 29 pinned
outcomes total. Chromium additionally inspects both the centered dot path and a
full-cell square finder path while preserving the existing real-size, refitting,
centering, deterministic-download, zero-request, and zero-retry workflows.

The development policy, testing strategy, release-hardening/readiness runbooks,
and local implementation map now record the final counts and bounded adverse
claims. Release automation merges and validates both artifact formats without
restoring manual evidence, bundle-size testing, the removed accessibility test,
or a performance benchmark.
