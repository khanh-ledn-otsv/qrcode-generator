[project]
name = "qr-fixture-oracles"
version = "0.1.0"
requires-python = ">=3.9,<4"
dependencies = [
    "qrcode==8.2",
    "qrcodegen==1.8.0",
]

[dependency-groups]
dev = [
    "cmake==4.4.2",
    "ruff==0.16.1",
    "ty==0.0.69",
]

[tool.uv]
package = false
